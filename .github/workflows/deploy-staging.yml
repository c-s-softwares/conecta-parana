name: Deploy Staging

on:
  workflow_dispatch:
    inputs:
      target:
        description: "Frente para deploy:"
        required: true
        default: todas
        type: choice
        options:
          - backend
          - admin
          - mobile
          - todas

permissions:
  contents: write
  packages: write

jobs:
  deploy-db:
    name: deploy-db (staging)
    if: ${{ inputs.target == 'backend' || inputs.target == 'todas' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login no GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build e push imagem db
        uses: docker/build-push-action@v5
        with:
          context: ./infra
          file: ./infra/Dockerfile.postgis
          push: true
          platforms: linux/arm64
          tags: ghcr.io/${{ github.repository_owner }}/conecta-parana-db:staging

      - name: Deploy DB na VM via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /opt/conecta-parana/staging
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose -f docker-compose.staging.yml pull db
            docker compose -f docker-compose.staging.yml up -d db

  deploy-backend:
    name: deploy-backend (staging)
    if: ${{ inputs.target == 'backend' || inputs.target == 'todas' }}
    needs: deploy-db
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login no GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build e push imagem backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          platforms: linux/arm64
          tags: ghcr.io/${{ github.repository_owner }}/conecta-parana-backend:staging

      - name: Deploy na VM via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /opt/conecta-parana/staging
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose -f docker-compose.staging.yml pull backend
            docker compose -f docker-compose.staging.yml up -d backend
            docker compose -f docker-compose.staging.yml exec -T backend npx prisma migrate deploy

  deploy-admin:
    name: deploy-admin (staging)
    if: ${{ inputs.target == 'admin' || inputs.target == 'todas' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login no GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build e push imagem admin
        uses: docker/build-push-action@v5
        with:
          context: ./admin
          file: ./admin/Dockerfile
          push: true
          platforms: linux/arm64
          build-args: |
            ENVIRONMENT=staging
          tags: ghcr.io/${{ github.repository_owner }}/conecta-parana-admin:staging

      - name: Deploy na VM via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /opt/conecta-parana/staging
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose -f docker-compose.staging.yml pull admin
            docker compose -f docker-compose.staging.yml up -d admin

  build-mobile:
    name: build-mobile (staging)
    if: ${{ inputs.target == 'mobile' || inputs.target == 'todas' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Insttodas dependencies
        working-directory: mobile
        run: flutter pub get

      - name: Build APK (dev)
        working-directory: mobile
        run: flutter build apk --flavor dev -t lib/main_dev.dart --dart-define=API_BASE_URL=https://api-staging.conectaparana.com.br

      - name: Criar GitHub Release (pre-release)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: staging-${{ github.sha }}
          name: "Staging — ${{ github.sha }}"
          prerelease: true
          body: |
            Build de teste gerada via deploy manual de staging.
            Aponta para: https://api-staging.conectaparana.com.br
            Commit: ${{ github.sha }}
            Disparado por: ${{ github.actor }}
          files: |
            mobile/build/app/outputs/flutter-apk/app-dev-release.apk